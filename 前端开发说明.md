# Local Dev Workspace — 前端开发说明

> 基于 `架构说明.md` 提取，结合项目现有代码现状，生成具体可执行的前端开发规范与任务拆解。

---

## 一、项目定位

开发一个运行在 **macOS 本机** 上的 **Local Dev Workspace（本地开发工作台）** Web 应用。

目标：通过浏览器（`http://127.0.0.1:xxxx`）统一管理本机开发环境中的：

1. 配置文件（Config Files）
2. 日志文件（Log Files）
3. 本地应用进程（Applications）
4. Git 项目（Git Projects）
5. Git 项目分组（Git Groups）
6. 项目对应的 RAG / Index 工作目录（Workspace）

---

## 二、技术栈（已具备）

| 类别 | 选型 | 版本 |
|------|------|------|
| 框架 | React | ^19.2 |
| 语言 | TypeScript | ~6.0 |
| 构建 | Vite | ^8.0 |
| UI 组件库 | Ant Design | ^6.4 |
| 图标 | @ant-design/icons | 6.x |
| 路由 | react-router | ^8.0 |
| 代码编辑器 | CodeMirror（已有） / Monaco Editor（待引入） | — |
| 服务端状态 | TanStack Query（待引入） | — |
| 客户端状态 | Zustand（待引入） | — |
| 样式 | Less | ^4.6 |
| 包管理 | pnpm | — |

---

## 三、现有代码盘点

### 3.1 目录结构（当前）

```
src/
├── assets/
│   ├── hero.png
│   ├── img/fufu.png          # 卡片页面使用的立绘
│   ├── react.svg
│   └── svg/
│       ├── menu.svg
│       └── vite.svg
├── components/
│   └── CustomIcom/index.tsx   # 自定义图标组件（支持 SVG / 图片）
├── layout/
│   ├── index.tsx              # 主布局：侧边栏 + Content + Outlet
│   ├── index.less
│   └── menu/
│       ├── index.tsx          # 动态菜单组件（从路由配置自动生成）
│       ├── index.less
│       └── types.d.ts         # 菜单类型定义
├── pages/
│   ├── card/                  # ⚠️ 卡片页面 — 不可修改
│   │   ├── index.tsx
│   │   └── index.less
│   ├── home/
│   │   ├── index.tsx          # Vite 模板页（可替换）
│   │   ├── index.css
│   │   └── kanhban/
│   │       ├── index.tsx      # Markdown 编辑器（CodeMirror + 预览）
│   │       └── index.less
│   └── login/
│       └── index.tsx          # 登录占位页
├── router/
│   ├── routes.ts              # 路由入口 & layoutRoute 组装
│   ├── types.d.ts             # Route 类型定义
│   ├── card/index.ts          # 卡片路由
│   ├── home/index.ts          # 主页路由（含子路由）
│   └── user/index.ts          # 登录路由
├── index.css
└── main.tsx                   # 应用入口
```

### 3.2 已有基础设施

**Layout（`src/layout/index.tsx`）**
- Ant Design `Layout` 组件：左侧 `Sider`（可折叠，折叠宽度 50px）+ 右侧 `Content`
- 通过 `<Outlet />` 渲染子路由
- 折叠按钮使用自定义 SVG 图标（`CustomIcon`）

**Menu（`src/layout/menu/index.tsx`）**
- 从 `layoutRoute.children` 自动生成菜单项
- 支持嵌套子菜单（通过 `route.children`）
- 自动匹配当前路径高亮选中项
- 自动展开当前路径的父级菜单组
- 菜单项按 `route.key` 排序
- `showInMenu: false` 的路由不出现在菜单中

**Router（`src/router/routes.ts`）**
- 使用 `createBrowserRouter`
- 自定义 `Route` 类型扩展了 `RouteObject`，增加 `name`、`icon`、`key`、`showInMenu`
- 按模块拆分路由文件：`home`、`card`、`user`
- 支持 `lazy()` 懒加载
- 根路径 `/` 默认跳转到 `/home/info`

**CustomIcon（`src/components/CustomIcom/index.tsx`）**
- 通过 `import.meta.glob` 批量导入 SVG / 图片
- 使用 Ant Design 的 `Icon` 组件包装，与 antd Menu 无缝集成
- 支持 `type: 'svg' | 'img'`

### 3.3 ⚠️ 不可修改的部分

**`src/pages/card/` 整个目录不可修改。** 这是卡片菜单页面，保留原样不动。

### 3.4 需要重构/替换的部分

| 文件/目录 | 当前状态 | 目标 |
|-----------|---------|------|
| `src/pages/home/index.tsx` | Vite 模板页 | 替换为 Dashboard |
| `src/pages/home/kanhban/` | Markdown 编辑器 | 可保留作为"看板"页，或迁移到 Workspace |
| `src/pages/login/` | 占位页 | V1 不做登录，可移除或保留占位 |
| `src/router/home/` | 旧路由结构 | 重构为 Dashboard + 各模块路由 |

---

## 四、路由设计（目标）

```
/                                   → 重定向到 /dashboard
├── /dashboard                      → Dashboard 首页
│
├── /configurations                 → 配置文件列表
│   └── /configurations/:id         → 配置文件详情（Monaco 编辑器）
│
├── /logs                           → 日志列表
│   ├── /logs/groups                → 日志分组管理
│   └── /logs/:id                   → 日志详情（终端风格查看器）
│
├── /applications                   → 应用列表
│   ├── /applications/new           → 新建应用
│   └── /applications/:id           → 应用详情（状态、stdout/stderr）
│
├── /git                            → Git 项目管理
│   ├── /git/projects               → 项目列表 + 扫描
│   ├── /git/projects/:id           → 项目详情
│   ├── /git/groups                 → 分组列表
│   └── /git/groups/:id             → 分组详情
│
├── /workspace                      → 工作空间
│   ├── /workspace/rag              → RAG 目录管理
│   └── /workspace/index            → Index 目录管理
│
├── /card                           → 卡片页（保持不变）
└── /settings                       → 设置页
```

---

## 五、目录结构（目标）

```
src/
├── api/                            # 🆕 API 层
│   ├── client.ts                   # 统一请求客户端（baseURL、拦截器、错误处理）
│   ├── configApi.ts                # 配置文件 API
│   ├── logApi.ts                   # 日志 API
│   ├── applicationApi.ts           # 应用 API
│   ├── gitApi.ts                   # Git 项目 API
│   └── workspaceApi.ts             # Workspace API
│
├── components/                     # 共享组件
│   ├── CustomIcom/                 # 已有，保留
│   │   └── index.tsx
│   └── PageContainer/             # 🆕 页面容器（统一标题、面包屑、操作栏）
│       └── index.tsx
│
├── hooks/                          # 🆕 自定义 Hooks
│   ├── usePolling.ts               # 轮询 hook（应用状态 5s 刷新）
│   └── useConfigFiles.ts           # TanStack Query hooks（按模块拆分）
│
├── stores/                         # 🆕 Zustand 状态（仅 UI 状态）
│   └── uiStore.ts                  # sidebar 折叠、主题、编辑器设置等
│
├── types/                          # 🆕 全局类型定义
│   ├── api.ts                      # API 响应类型
│   ├── config.ts                   # 配置文件类型
│   ├── log.ts                      # 日志类型
│   ├── application.ts              # 应用类型
│   ├── git.ts                      # Git 项目类型
│   └── workspace.ts                # Workspace 类型
│
├── utils/                          # 🆕 工具函数
│   ├── path.ts                     # 路径处理（~ 展开、normalize）
│   └── format.ts                   # 日期/文件大小格式化
│
├── layout/                         # 保留并完善
│   ├── index.tsx
│   ├── index.less
│   └── menu/
│       ├── index.tsx
│       ├── index.less
│       └── types.d.ts
│
├── router/                         # 重构
│   ├── routes.ts                   # 路由入口
│   ├── types.d.ts                  # Route 类型
│   ├── card/                       # 卡片路由（保持不变）
│   │   └── index.ts
│   └── modules/                    # 🆕 按模块拆分
│       ├── dashboard.ts
│       ├── configurations.ts
│       ├── logs.ts
│       ├── applications.ts
│       ├── git.ts
│       ├── workspace.ts
│       └── settings.ts
│
├── pages/                          # 重构
│   ├── card/                       # ⚠️ 不可修改
│   │   ├── index.tsx
│   │   └── index.less
│   ├── dashboard/                  # 🆕 Dashboard
│   │   ├── index.tsx
│   │   └── index.less
│   ├── configurations/             # 🆕 配置管理
│   │   ├── list.tsx                # 列表页
│   │   ├── detail.tsx              # 详情页（Monaco 编辑器）
│   │   └── index.less
│   ├── logs/                       # 🆕 日志管理
│   │   ├── list.tsx                # 日志列表
│   │   ├── groups.tsx              # 分组管理
│   │   ├── detail.tsx              # 日志查看器
│   │   └── index.less
│   ├── applications/               # 🆕 应用管理
│   │   ├── list.tsx                # 应用列表
│   │   ├── form.tsx                # 新建/编辑表单
│   │   ├── detail.tsx              # 应用详情
│   │   └── index.less
│   ├── git/                        # 🆕 Git 管理
│   │   ├── projects.tsx            # 项目列表 + 扫描
│   │   ├── projectDetail.tsx       # 项目详情
│   │   ├── groups.tsx              # 分组列表
│   │   ├── groupDetail.tsx         # 分组详情
│   │   └── index.less
│   ├── workspace/                  # 🆕 Workspace
│   │   ├── rag.tsx
│   │   ├── index.tsx               # Index 目录
│   │   └── index.less
│   └── settings/                   # 🆕 设置
│       └── index.tsx
│
├── index.css
└── main.tsx
```

---

## 六、开发规范

### 6.1 路由注册规范

每个模块在 `src/router/modules/` 下创建一个文件，导出一个 `Route` 对象：

```typescript
// src/router/modules/dashboard.ts
import type { Route } from '@/router/types';
import { DashboardOutlined } from '@ant-design/icons';
import { createElement, lazy } from 'react';

const Dashboard = lazy(() => import('@/pages/dashboard'));

const routes: Route = {
  name: 'Dashboard',
  icon: createElement(DashboardOutlined),
  key: 1,
  path: '/dashboard',
  Component: Dashboard,
};

export default routes;
```

**规则：**
- `key` 决定菜单排序，按 10 的步长递增便于中间插入（10, 20, 30, 40, 50, 60, 70）
- `name` 是菜单显示文本
- 使用 `lazy()` 懒加载页面组件
- `showInMenu: false` 隐藏不需要在菜单显示的路由

### 6.2 API 调用规范

**绝对不允许**在页面组件中直接调用 `fetch('/api/...')`。

所有 API 调用封装在 `src/api/` 目录下：

```typescript
// src/api/applicationApi.ts
import client from './client';

export const applicationApi = {
  list: () => client.get('/api/applications'),
  getById: (id: number) => client.get(`/api/applications/${id}`),
  create: (data: CreateApplicationRequest) => client.post('/api/applications', data),
  update: (id: number, data: UpdateApplicationRequest) => client.put(`/api/applications/${id}`, data),
  delete: (id: number) => client.delete(`/api/applications/${id}`),
  start: (id: number) => client.post(`/api/applications/${id}/start`),
  stop: (id: number) => client.post(`/api/applications/${id}/stop`),
  restart: (id: number) => client.post(`/api/applications/${id}/restart`),
  status: (id: number) => client.get(`/api/applications/${id}/status`),
};
```

### 6.3 统一 API 响应格式

后端返回格式：

```typescript
// 成功（单条）
{ "data": { ... } }

// 成功（列表）
{ "data": { "items": [...], "total": 10 } }

// 错误
{ "error": { "code": "APPLICATION_NOT_FOUND", "message": "应用不存在" } }
```

前端 `client.ts` 需要处理：
- 自动解包 `data` 字段
- 统一错误提示（antd `message.error`）
- 请求/响应拦截

### 6.4 状态管理规范

```
Server State（服务端数据）  → TanStack Query
  ├── applications 列表/详情
  ├── configurations 列表/详情
  ├── logs 列表/内容
  ├── git projects/groups
  └── workspace 信息

UI State（客户端状态）     → Zustand
  ├── sidebar 折叠状态
  ├── 当前主题
  ├── 编辑器设置（字体大小、tabSize）
  └── 选中的日志/分组
```

### 6.5 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 页面组件 | PascalCase 目录 + `index.tsx` | `pages/dashboard/index.tsx` |
| API 模块 | `xxxApi.ts` | `configApi.ts` |
| 类型文件 | 模块名.ts | `types/application.ts` |
| Hook | `useXxx` | `usePolling.ts` |
| Store | `xxxStore.ts` | `uiStore.ts` |
| CSS class | kebab-case | `log-viewer` |

### 6.6 路径别名

已配置 `@` → `src/`，统一使用：

```typescript
import Layout from '@/layout';
import type { Route } from '@/router/types';
```

---

## 七、分阶段开发任务

### Phase 1：基础框架重构 ✅ 部分完成

**已完成：**
- [x] Layout（侧边栏 + Content）
- [x] 动态菜单（从路由配置生成）
- [x] 路由基础结构 + 模块拆分
- [x] CustomIcon 组件

**待完成：**
- [ ] 安装依赖：`@tanstack/react-query`、`zustand`、`axios`、`monaco-editor`
- [ ] 创建 `src/api/client.ts`（axios 实例，统一拦截器）
- [ ] 创建 `src/types/api.ts`（API 响应泛型类型）
- [ ] 创建 `src/stores/uiStore.ts`
- [ ] 将现有路由重构到 `src/router/modules/` 目录
- [ ] 重构 `src/router/routes.ts` 组装新路由
- [ ] 创建 `src/utils/format.ts`、`src/utils/path.ts`
- [ ] 移除/重构 Vite 模板页面（`home/index.tsx` → `dashboard/index.tsx`）
- [ ] 路由名称英文化（`/home` → `/dashboard`，`/login` 评估是否保留）

### Phase 2：配置文件管理

**页面：**
- [ ] `GET /api/config-files` → 配置文件列表页 `pages/configurations/list.tsx`
  - 表格：Name、Path、Description、Group、Updated At
  - 顶部 "+ Add" 按钮
  - 支持按 Group 筛选
- [ ] `GET /api/config-files/:id` → 配置文件详情页 `pages/configurations/detail.tsx`
  - Monaco Editor（根据文件后缀自动语法高亮：JSON/YAML/TOML/JS/TS/Shell）
  - 保存按钮 → `PUT /api/config-files/:id`
  - 编辑前备份提示
- [ ] `POST /api/config-files` → 新建配置对话框
  - 表单：Name、Path、Description、Group
  - 后端自动验证路径是否存在、是否可读
- [ ] `DELETE /api/config-files/:id` → 删除（仅删记录，不删文件，需确认弹窗）

**API 文件：**
- [ ] `src/api/configApi.ts`
- [ ] `src/types/config.ts`

**路由：**
- [ ] `/configurations` → 配置列表
- [ ] `/configurations/:id` → 配置详情

### Phase 3：日志管理

**页面：**
- [ ] `GET /api/log-groups` → 日志分组管理页 `pages/logs/groups.tsx`
- [ ] `GET /api/log-files` → 日志列表页 `pages/logs/list.tsx`
  - 左侧：分组树（All Logs + Groups）
  - 右侧：日志文件列表
- [ ] `GET /api/log-files/:id/content` → 日志详情页 `pages/logs/detail.tsx`
  - 终端风格 UI（黑底绿字）
  - 行号显示
  - 搜索 + 关键字高亮
  - Tail：只显示最后 N 行
  - Refresh 按钮手动刷新
  - 分页参数：`offset`、`limit`

**API 文件：**
- [ ] `src/api/logApi.ts`
- [ ] `src/types/log.ts`

**路由：**
- [ ] `/logs` → 日志列表
- [ ] `/logs/groups` → 日志分组
- [ ] `/logs/:id` → 日志详情

### Phase 4：应用管理（核心模块）

这是整个系统最重要的前端模块。

**页面：**
- [ ] `GET /api/applications` → 应用列表页 `pages/applications/list.tsx`
  - 表格：Name、Status（彩色圆点）、PID、Command、Action（Start/Stop/Restart 按钮）
  - 轮询刷新（5s 间隔），使用 `usePolling` hook
  - 状态颜色：Running=green、Stopped=gray、Failed=red、Starting/Stopping=orange
- [ ] `POST /api/applications` → 新建/编辑应用 `pages/applications/form.tsx`
  - 表单：Name、Description、Command、Working Directory、Environment（KV 编辑器）
  - Auto Start checkbox
- [ ] `GET /api/applications/:id` → 应用详情 `pages/applications/detail.tsx`
  - Overview：Name、Description、Command、Working Directory、Status、PID、Started At、Uptime
  - 操作按钮：Start、Stop、Restart
  - Process Output：stdout / stderr 切换显示
- [ ] 启动/停止/重启确认与状态反馈（antd `message` / `notification`）

**API 文件：**
- [ ] `src/api/applicationApi.ts`
- [ ] `src/types/application.ts`

**Hook：**
- [ ] `src/hooks/usePolling.ts` — 通用轮询 hook

**路由：**
- [ ] `/applications` → 应用列表
- [ ] `/applications/new` → 新建应用
- [ ] `/applications/:id` → 应用详情

### Phase 5：Git 项目管理

**页面：**
- [ ] `POST /api/git/projects/scan` → 扫描目录 `pages/git/projects.tsx`
  - 顶部：路径输入框 + Scan 按钮
  - 扫描结果列表：Name、Path、Branch、Remote、Group
  - 将扫描到的项目保存到数据库
- [ ] `GET /api/git/projects` → 保存的项目列表
- [ ] `GET /api/git/projects/:id` → 项目详情 `pages/git/projectDetail.tsx`
  - Overview：Name、Path、Branch、Remote、Last Commit、Status
  - Git 状态标签：Clean / Modified / Untracked / Ahead / Behind
  - AI Context 区域：Group、RAG Path、Index Path
- [ ] `GET /api/git/groups` → 分组列表 `pages/git/groups.tsx`
  - 卡片形式：Group Name、项目数、RAG Path、Index Path
- [ ] `GET /api/git/groups/:id` → 分组详情 `pages/git/groupDetail.tsx`
  - 该分组下的项目列表
  - RAG Directory、Index Directory 路径展示

**API 文件：**
- [ ] `src/api/gitApi.ts`
- [ ] `src/types/git.ts`

**路由：**
- [ ] `/git/projects` → 项目列表
- [ ] `/git/projects/:id` → 项目详情
- [ ] `/git/groups` → 分组列表
- [ ] `/git/groups/:id` → 分组详情

### Phase 6：Workspace 管理

**页面：**
- [ ] `pages/workspace/rag.tsx` — RAG 目录浏览（基于 workspace 文件系统）
- [ ] `pages/workspace/index.tsx` — Index 目录浏览

**路由：**
- [ ] `/workspace/rag`
- [ ] `/workspace/index`

### Phase 7：设置页（可后续补充）

- [ ] `pages/settings/index.tsx`
  - 后端地址配置
  - 默认扫描目录
  - 主题设置

---

## 八、关键组件说明

### 8.1 菜单系统

菜单从路由配置自动生成，规则如下：

```
route.children 中的每个 Route：
  - showInMenu !== false → 出现在菜单中
  - 有 children → 渲染为 SubMenu 组
  - 无 children → 渲染为 MenuItem，点击导航到 route.path
  - 按 route.key 升序排列
```

菜单与路由完全解耦 — 添加新页面只需：
1. 在 `src/router/modules/` 创建路由文件
2. 在 `src/router/routes.ts` 的 `layoutRoute.children` 中引入
3. 菜单自动生成，无需手动配置

### 8.2 Icon 组件

使用 `CustomIcon` 组件支持项目自定义 SVG 图标：

```tsx
<CustomIcon iconName="menu" type="svg" iconWidth={20} iconHeight={20} />
```

新增自定义图标：将 SVG 文件放入 `src/assets/svg/`，直接通过 `iconName` 引用（不含扩展名）。

---

## 九、V1 明确不做的事情

为了控制复杂度，第一版不实现：

- Docker / Kubernetes 管理
- 远程服务器管理 / SSH
- 远程 Git 操作
- AI Chat / Embedding / Vector Database
- 复杂权限系统 / 用户系统 / 账号登录
- 云同步
- WebSocket / SSE 实时推送（第二版再加）

---

## 十、开发注意事项

1. **轮询策略**：应用状态使用 5s 轮询，前端使用 `usePolling` hook 封装，后续可平滑迁移到 SSE/WebSocket
2. **路径展示**：用户输入的 `~` 路径发给后端处理，前端接收绝对路径后展示时可还原为 `~` 前缀（通过 `utils/path.ts`）
3. **大文件处理**：日志内容使用分页加载（`offset` + `limit`），不要一次性请求完整文件
4. **Monaco Editor**：配置文件编辑页使用 Monaco Editor，按文件后缀自动设置语言模式
5. **错误处理**：所有 API 调用在 `client.ts` 中统一错误拦截，按 error code 分类提示
6. **确认操作**：删除、停止应用、覆盖配置文件等破坏性操作，使用 antd `Modal.confirm` 确认
7. **响应式**：侧边栏折叠状态已实现，Content 区域自适应
8. **命令执行安全**：前端只做展示，后端处理所有路径校验和命令执行安全

---

*最后更新：2026-08-10*
*基于：架构说明.md v1.0*
