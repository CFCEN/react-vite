可以。这个项目我建议你不要把它设计成一个普通的 CRUD 管理后台，而是定位成：

Local Dev Workspace / 本地开发工作台

用一个 Web UI 统一管理本机配置文件、日志、应用进程、Git 项目，以及未来的 AI/RAG 开发上下文。

你这个场景里，React + Go + SQLite 非常合适，而且我建议从第一版开始就把「文件、进程、Git、索引目录」这些能力抽象好，否则后面接 AI 会比较难扩展。

下面这份可以直接丢给 Codex 作为开发说明书。

Local Dev Workspace

React + Go 本地开发管理平台开发说明书

⸻

一、项目定位

开发一个运行在 macOS 本机上的本地开发工作台 Web 应用。

目标是通过浏览器统一管理本机开发环境中的：

1. 配置文件
2. 日志文件
3. 本地应用进程
4. Git 项目
5. Git 项目分组
6. 项目对应的 RAG / Index 工作目录
7. 后续扩展 AI 项目分析、代码修改、项目搜索能力

项目采用：

Frontend
React + TypeScript + Vite
        │
        │ HTTP / JSON
        ▼
Backend
Go
        │
        ├── SQLite
        ├── Local File System
        ├── Process Manager
        ├── Git
        └── RAG / Index Workspace

⸻

二、核心设计原则

2.1 本项目是 Local First

所有核心数据都保存在本机：

Browser
   │
   ▼
Go Local Server
   │
   ├── SQLite
   ├── 本地配置文件
   ├── 本地日志
   ├── 本地 Git 项目
   └── 本地进程

默认只允许：

127.0.0.1
localhost

访问后端。

不要设计成远程服务器架构。

⸻

三、为什么使用 SQLite，而不是 MongoDB

第一版明确使用：

SQLite

不使用 MongoDB。

原因：

1. 数据结构高度结构化

核心数据都是：

配置文件
日志文件
应用
应用分组
Git 项目
Git 分组
RAG 配置

这些天然适合关系数据库。

2. 本地应用没有 MongoDB 的部署价值

如果使用 MongoDB：

Local App
   │
   ▼
MongoDB
   │
   └── Docker / Homebrew / 独立服务

会增加用户本地环境依赖。

SQLite：

Local App
   │
   ▼
workspace.db

即可。

3. 后续数据量不会特别大

这个系统保存的是：

metadata
configuration
process state
project information
group information

并不是日志正文或者代码全文。

所以 SQLite 完全足够。

⸻

四、推荐项目结构

local-dev-workspace/
│
├── frontend/
│   ├── src/
│   │   ├── api/
│   │   ├── components/
│   │   ├── layouts/
│   │   ├── pages/
│   │   ├── hooks/
│   │   ├── stores/
│   │   ├── types/
│   │   ├── utils/
│   │   └── router/
│   │
│   ├── package.json
│   └── vite.config.ts
│
├── backend/
│   ├── cmd/
│   │   └── server/
│   │       └── main.go
│   │
│   ├── internal/
│   │   ├── api/
│   │   ├── service/
│   │   ├── repository/
│   │   ├── model/
│   │   ├── process/
│   │   ├── filesystem/
│   │   ├── git/
│   │   ├── rag/
│   │   └── config/
│   │
│   ├── migrations/
│   ├── data/
│   │   └── workspace.db
│   └── go.mod
│
├── workspace/
│   ├── rag/
│   └── index/
│
└── README.md

⸻

五、后端总体架构

Go 后端建议采用：

HTTP Router
     │
     ▼
Handler
     │
     ▼
Service
     │
     ├───────────────┐
     ▼               ▼
Repository       System Service
     │               │
     ▼               ├── FileSystem
   SQLite            ├── Process
                     ├── Git
                     └── RAG

不要让 HTTP Handler 直接操作 SQLite 或 OS。

例如：

错误：

func GetConfig(w http.ResponseWriter, r *http.Request) {
    os.ReadFile(...)
}

正确：

Handler
  ↓
ConfigService
  ↓
FileService

⸻

六、数据库设计

6.1 config_files

配置文件。

CREATE TABLE config_files (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    path TEXT NOT NULL UNIQUE,
    description TEXT,
    group_name TEXT,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL
);

例如：

Claude Config
~/.claude/settings.json
Vite Config
~/project/vite.config.ts
Git Config
~/.gitconfig

⸻

七、日志文件

log_files

CREATE TABLE log_files (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    path TEXT NOT NULL UNIQUE,
    group_id INTEGER,
    description TEXT,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL
);

log_groups

CREATE TABLE log_groups (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    description TEXT,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL
);

关系：

LogGroup
   │
   ├── Log File
   ├── Log File
   └── Log File

⸻

八、本地应用

这是整个系统非常重要的模块。

applications

CREATE TABLE applications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT,
    command TEXT NOT NULL,
    working_directory TEXT,
    environment TEXT,
    auto_start INTEGER NOT NULL DEFAULT 0,
    enabled INTEGER NOT NULL DEFAULT 1,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL
);

其中：

command

保存真正执行的命令。

例如：

pnpm dev

或者：

go run ./cmd/server

或者：

npm run dev

⸻

九、进程运行状态

不要把 PID 永久存储为唯一状态。

因为：

PID 可能被操作系统重新分配

数据库可以保存：

last_pid

但是实际运行状态必须由 Go 后端实时检查。

建议增加：

CREATE TABLE application_runs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    application_id INTEGER NOT NULL,
    pid INTEGER,
    started_at DATETIME,
    stopped_at DATETIME,
    exit_code INTEGER,
    status TEXT,
    created_at DATETIME NOT NULL
);

status：

STARTING
RUNNING
STOPPING
STOPPED
FAILED
UNKNOWN

⸻

十、应用启动机制

点击：

启动

前端：

POST /api/applications/:id/start

Go：

ApplicationService
        ↓
ProcessManager
        ↓
os/exec
        ↓
Command
        ↓
Process
        ↓
PID

例如：

pnpm dev

启动后：

PID = 12345

返回：

{
  "id": 1,
  "pid": 12345,
  "status": "RUNNING"
}

⸻

十一、必须处理子进程问题

不能简单：

exec.Command("pnpm", "dev")

然后只记录 PID。

因为：

pnpm
 ↓
node
 ↓
vite

真正运行的可能不是最初 PID。

ProcessManager 必须支持：

Process Tree

停止应用时应该尽可能停止整个进程树。

例如：

Application
   │
   └── PID 100
        │
        ├── PID 101
        └── PID 102

停止：

PID 100
PID 101
PID 102

而不是只 kill 100。

⸻

十二、应用状态检测

后端维护：

ApplicationMonitor

每：

5 seconds

扫描一次运行中的应用。

逻辑：

for application in runningApplications:
    pid := application.pid
    processExists(pid)
        │
        ├── true
        │      ↓
        │    RUNNING
        │
        └── false
               ↓
             STOPPED

同时提供：

GET /api/applications/:id/status

前端不需要每 5 秒请求一次。

推荐：

Backend 5s monitor
        ↓
Frontend 5~10s polling

第一版可以简单采用：

Frontend 每 5 秒刷新一次状态

后续再增加 WebSocket/SSE。

⸻

十三、应用标准输出

强烈建议第一版就设计：

stdout
stderr

虽然第一版 UI 可以不展示，但后端需要支持。

启动：

Application
     │
     ├── stdout
     └── stderr

可以保存到：

workspace/applications/{id}/stdout.log
workspace/applications/{id}/stderr.log

这样后续可以直接在应用详情页面查看启动日志。

⸻

十四、配置文件 API

获取配置列表

GET /api/config-files

返回：

{
  "items": [
    {
      "id": 1,
      "name": "Claude Config",
      "path": "~/.claude/settings.json",
      "description": "",
      "groupName": "AI"
    }
  ]
}

⸻

获取配置详情

GET /api/config-files/:id

返回：

{
  "id": 1,
  "name": "Claude Config",
  "path": "/Users/user/.claude/settings.json",
  "content": "{...}",
  "size": 1024,
  "modifiedAt": "2026-08-10T10:00:00Z"
}

⸻

新增

POST /api/config-files

请求：

{
  "name": "Claude Config",
  "path": "~/.claude/settings.json",
  "description": "Claude configuration"
}

后端必须验证：

path 是否存在
是否为文件
是否有读取权限

⸻

修改配置

PUT /api/config-files/:id

请求：

{
  "content": "{...}"
}

后端：

读取旧文件
      ↓
创建 backup
      ↓
写入临时文件
      ↓
rename

不要直接：

os.WriteFile()

避免写文件过程中程序崩溃导致配置文件损坏。

⸻

删除配置记录

DELETE /api/config-files/:id

注意：

只删除数据库记录，不删除本地文件。

⸻

十五、日志 API

日志分组

GET /api/log-groups
POST /api/log-groups
PUT /api/log-groups/:id
DELETE /api/log-groups/:id

⸻

日志文件

GET /api/log-files
GET /api/log-files/:id
POST /api/log-files
PUT /api/log-files/:id
DELETE /api/log-files/:id

⸻

查看日志内容

GET /api/log-files/:id/content

支持：

offset
limit
tail

例如：

GET /api/log-files/1/content?tail=500

返回：

{
  "content": "...",
  "size": 102400,
  "modifiedAt": "..."
}

不要一次性读取巨大日志文件。

⸻

十六、日志 Tail

后续支持：

GET /api/log-files/:id/tail

使用：

offset

或者 SSE：

GET /api/log-files/:id/stream

第一版可以先实现普通读取。

第二版实现：

实时日志

⸻

十七、应用 API

获取应用列表

GET /api/applications

⸻

创建应用

POST /api/applications

请求：

{
  "name": "Aisee Frontend",
  "description": "Aisee frontend",
  "command": "pnpm dev",
  "workingDirectory": "/Users/user/project/aisee",
  "environment": {
    "NODE_ENV": "development"
  }
}

⸻

修改应用

PUT /api/applications/:id

⸻

删除应用

DELETE /api/applications/:id

删除前：

如果应用正在运行
        ↓
先停止
        ↓
再删除

⸻

十八、启动应用

POST /api/applications/:id/start

返回：

{
  "applicationId": 1,
  "pid": 12345,
  "status": "RUNNING"
}

⸻

十九、停止应用

POST /api/applications/:id/stop

⸻

二十、重启应用

POST /api/applications/:id/restart

实现：

stop
 ↓
wait
 ↓
start

⸻

二十一、应用状态

GET /api/applications/:id/status

返回：

{
  "id": 1,
  "pid": 12345,
  "status": "RUNNING",
  "startedAt": "2026-08-10T10:00:00Z"
}

⸻

二十二、Git 项目发现

用户可以指定：

/Users/user/workspace

系统扫描：

workspace/
├── project-a/
│   └── .git
│
├── project-b/
│   └── .git
│
├── group/
│   ├── project-c/
│   │   └── .git
│   │
│   └── project-d/
│       └── .git

发现：

project-a
project-b
project-c
project-d

⸻

二十三、Git Scan API

POST /api/git/projects/scan

请求：

{
  "path": "/Users/user/workspace",
  "maxDepth": 5
}

返回：

{
  "items": [
    {
      "name": "project-a",
      "path": "/Users/user/workspace/project-a",
      "branch": "main",
      "remote": "git@github.com:xxx/project-a.git"
    }
  ]
}

⸻

二十四、Git 项目

建议保存发现过的项目。

git_projects

CREATE TABLE git_projects (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    path TEXT NOT NULL UNIQUE,
    branch TEXT,
    remote TEXT,
    group_id INTEGER,
    rag_path TEXT,
    index_path TEXT,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL
);

⸻

二十五、Git 项目分组

git_groups

CREATE TABLE git_groups (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    description TEXT,
    rag_path TEXT,
    index_path TEXT,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL
);

关系：

Git Group
    │
    ├── Project A
    ├── Project B
    ├── Project C
    │
    ├── rag/
    │
    └── index/

⸻

二十六、RAG / Index 目录设计

这是后续 AI 功能的重要基础。

建议不要把：

rag
index

直接放在 Git 项目内部。

而是：

workspace/
│
├── rag/
│   ├── group-a/
│   │
│   └── group-b/
│
└── index/
    ├── group-a/
    │
    └── group-b/

例如：

workspace/
├── rag/
│   └── frontend/
│
└── index/
    └── frontend/

Group：

frontend

包含：

project-a
project-b
project-c

那么：

rag/frontend/
index/frontend/

服务于整个项目组。

⸻

二十七、为什么 RAG 和 Index 使用 Group 级别

因为未来 AI 修改项目时，往往不是：

只理解一个项目

而是：

项目 A
项目 B
公共组件
公共 SDK
后端服务
前端服务

例如：

frontend-group
├── admin-web
├── user-web
├── shared-ui
└── api-sdk

AI 可以通过：

RAG
+
Index

建立整个业务域的上下文。

⸻

二十八、Git Group API

GET /api/git/groups
POST /api/git/groups
PUT /api/git/groups/:id
DELETE /api/git/groups/:id

⸻

二十九、Git Project API

GET /api/git/projects
GET /api/git/projects/:id
PUT /api/git/projects/:id
DELETE /api/git/projects/:id

⸻

三十、项目分组

POST /api/git/projects/:id/group

请求：

{
  "groupId": 1
}

后端：

Project
   ↓
Group
   ↓
ensure rag directory
   ↓
ensure index directory

如果目录不存在：

mkdir -p

自动创建。

⸻

三十一、Git 项目详情

项目详情需要展示：

项目名称
项目路径
Git Branch
Git Remote
Git 状态
最后 Commit
项目所属 Group
RAG Directory
Index Directory

Git 状态可以展示：

Clean
Modified
Untracked
Ahead
Behind

⸻

三十二、Git 项目扫描优化

不要每次打开页面都递归扫描整个目录。

建议：

用户主动 Scan
       ↓
发现项目
       ↓
保存 SQLite
       ↓
页面读取 SQLite

提供：

扫描目录
重新扫描
刷新项目

⸻

三十三、Frontend 页面结构

一级菜单建议：

Dashboard
Configuration
Logs
Applications
Git Projects
Workspace
Settings

完整：

/
├── Dashboard
│
├── Configurations
│   ├── Config List
│   └── Config Detail
│
├── Logs
│   ├── Log Groups
│   ├── Log List
│   └── Log Detail
│
├── Applications
│   ├── Application List
│   └── Application Detail
│
├── Git Projects
│   ├── Projects
│   ├── Groups
│   └── Project Detail
│
├── Workspace
│   ├── RAG
│   └── Index
│
└── Settings

⸻

三十四、Dashboard

首页不是简单统计卡片。

应该成为：

Local Development Dashboard

展示：

Running Applications

例如：

Aisee Frontend       ● Running
Backend API          ● Running
Redis                ● Running

⸻

最近项目

Recent Projects

展示：

Project
Branch
Status
Last Open

⸻

最近日志

Recent Logs

⸻

配置文件

Recently Modified Configs

⸻

三十五、Configuration 页面

一级：

Configurations

二级：

All
Groups

列表：

┌──────────────────────────────────────────────┐
│ Configurations                    + Add      │
├──────────────────────────────────────────────┤
│ Name       Path                 Updated      │
│ Claude     ~/.claude/...        10:20        │
│ Vite       ~/project/...        09:30        │
│ Git        ~/.gitconfig         Yesterday    │
└──────────────────────────────────────────────┘

点击：

Config Detail

右侧或者独立页面使用代码编辑器。

推荐：

Monaco Editor

支持：

JSON
YAML
TOML
JS
TS
Shell

根据文件后缀自动识别。

⸻

三十六、日志页面

一级：

Logs

左侧：

All Logs
Groups
├── Backend
├── Frontend
├── Docker
└── Kubernetes

右侧：

Log Files

点击日志：

Log Detail

采用类似：

terminal

的 UI。

支持：

搜索
关键字高亮
行号
Tail
Refresh

后续：

Live

实时查看。

⸻

三十七、Applications 页面

这是最核心页面之一。

列表：

Applications
┌─────────────────────────────────────────────────────────┐
│ Name          Status      PID       Command      Action │
├─────────────────────────────────────────────────────────┤
│ Frontend      ● Running   12345     pnpm dev     Stop  │
│ Backend       ● Running   23456     go run       Stop  │
│ Redis         ○ Stopped   -         redis-server Start │
└─────────────────────────────────────────────────────────┘

⸻

三十八、Application Detail

详情：

Application
────────────────────
Name
Description
Command
Working Directory
Environment
Status
PID
Started At
Process Uptime

按钮：

Start
Stop
Restart

下面：

Process Output

展示：

stdout
stderr

⸻

三十九、Git Projects 页面

一级：

Git Projects

顶部：

Scan Directory

例如：

[ /Users/user/workspace ] [Scan]

扫描后：

Projects
Project A
/Users/user/workspace/project-a
main
frontend
Project B
/Users/user/workspace/project-b
develop
backend

⸻

四十、Git Groups

Group 页面：

Groups
Frontend
  5 projects
Backend
  8 projects
Infrastructure
  4 projects

点击：

Frontend

进入：

Frontend Group

展示：

Projects
admin-web
user-web
shared-ui
api-sdk

以及：

RAG
/xxx/workspace/rag/frontend
Index
/xxx/workspace/index/frontend

⸻

四十一、Project Detail

页面结构：

Project
│
├── Overview
├── Git
├── Files
├── Group
└── AI Context

Overview：

Name
Path
Branch
Remote
Last Commit
Status

AI Context：

Group
RAG Path
Index Path

未来可以增加：

Build Index
Rebuild Index
Ask AI
AI Modify

⸻

四十二、前端技术栈

推荐：

React
TypeScript
Vite
React Router
Ant Design
TanStack Query
Zustand
Monaco Editor

项目 UI 可以继续使用 Ant Design。

⸻

四十三、前端状态管理

不要所有状态都放 Zustand。

区分：

Server State

使用：

TanStack Query

例如：

applications
configurations
logs
git projects
groups

UI State

使用：

Zustand

例如：

sidebar collapsed
selected log
editor settings
theme

⸻

四十四、API Client

统一：

src/api/

例如：

configApi.ts
logApi.ts
applicationApi.ts
gitApi.ts
workspaceApi.ts

不要页面中直接：

fetch('/api/...')

应该：

applicationApi.start(id)

⸻

四十五、统一 API Response

后端建议统一：

成功：

{
  "data": {}
}

列表：

{
  "data": {
    "items": [],
    "total": 10
  }
}

错误：

{
  "error": {
    "code": "FILE_NOT_FOUND",
    "message": "File does not exist"
  }
}

⸻

四十六、错误码

建议：

FILE_NOT_FOUND
FILE_NOT_READABLE
FILE_NOT_WRITABLE
INVALID_PATH
INVALID_COMMAND
APPLICATION_NOT_FOUND
APPLICATION_ALREADY_RUNNING
APPLICATION_NOT_RUNNING
APPLICATION_START_FAILED
APPLICATION_STOP_FAILED
GIT_PROJECT_NOT_FOUND
GIT_SCAN_FAILED
GROUP_NOT_FOUND

⸻

四十七、路径处理

macOS 路径必须支持：

~
$HOME
absolute path

例如：

~/.claude/settings.json

后端统一 normalize：

~/
 ↓
/Users/username/

数据库建议保存：

absolute canonical path

前端展示可以转换回：

~/

⸻

四十八、路径安全

后端所有文件操作必须：

Validate
Normalize
Check
Execute

禁止因为用户输入：

../../

导致任意目录越界。

尤其是：

日志读取
配置文件读取
Git Scan

⸻

四十九、命令执行安全

这个应用本身就是本地开发工具，因此需要支持用户配置命令。

但是必须明确：

Command execution is a privileged local capability.

后端默认：

127.0.0.1:xxxx

监听。

不要监听：

0.0.0.0

避免局域网其他设备直接调用：

/api/applications/:id/start

从而执行本机命令。

⸻

五十、命令执行模型

第一版允许：

command
workingDirectory
environment

例如：

Command:
pnpm dev
Working Directory:
/Users/user/project
Environment:
NODE_ENV=development

不要第一版就做复杂 Shell Parser。

可以通过：

/bin/zsh -lc "<command>"

执行。

因为用户的开发命令可能包含：

cd xxx && pnpm dev

或者：

source ~/.zshrc && xxx

但需要明确这是一个本地管理员工具。

⸻

五十一、应用生命周期

状态机：

STOPPED
   │
   │ start
   ▼
STARTING
   │
   ├──────────────┐
   │              │
   ▼              ▼
RUNNING         FAILED
   │
   │ stop
   ▼
STOPPING
   │
   ▼
STOPPED

⸻

五十二、后端后台任务

启动后：

ApplicationMonitor

负责：

每 5 秒检查运行中的进程

未来：

LogWatcher
GitWatcher
IndexBuilder
RAGBuilder

也可以加入统一：

BackgroundTaskManager

⸻

五十三、未来 AI 架构预留

第一版不要真正实现 AI。

但是数据库和目录结构必须预留：

Git Group
     │
     ├── Projects
     │
     ├── RAG Directory
     │
     └── Index Directory
              │
              ▼
        Future AI Service

未来可以增加：

POST /api/ai/index/build
POST /api/ai/query
POST /api/ai/project/modify

⸻

五十四、AI Context 数据模型预留

建议增加：

CREATE TABLE project_contexts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    group_id INTEGER NOT NULL,
    rag_path TEXT,
    index_path TEXT,
    index_status TEXT,
    last_indexed_at DATETIME,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL
);

状态：

NOT_INDEXED
INDEXING
READY
FAILED
OUTDATED

⸻

五十五、前后端 API 总览

/api
├── /config-files
│   ├── GET
│   ├── POST
│   ├── PUT /:id
│   └── DELETE /:id
│
├── /log-groups
│   ├── GET
│   ├── POST
│   ├── PUT /:id
│   └── DELETE /:id
│
├── /log-files
│   ├── GET
│   ├── POST
│   ├── GET /:id
│   ├── PUT /:id
│   ├── DELETE /:id
│   └── GET /:id/content
│
├── /applications
│   ├── GET
│   ├── POST
│   ├── PUT /:id
│   ├── DELETE /:id
│   ├── POST /:id/start
│   ├── POST /:id/stop
│   ├── POST /:id/restart
│   └── GET /:id/status
│
├── /git
│   ├── /projects
│   │   ├── GET
│   │   ├── POST /scan
│   │   ├── GET /:id
│   │   ├── PUT /:id
│   │   └── DELETE /:id
│   │
│   └── /groups
│       ├── GET
│       ├── POST
│       ├── PUT /:id
│       └── DELETE /:id
│
└── /workspace
    ├── GET
    ├── /rag
    └── /index

⸻

五十六、路由设计

前端：

/
├── dashboard
│
├── configurations
│   ├── /
│   ├── /new
│   └── /:id
│
├── logs
│   ├── /
│   ├── /groups
│   └── /:id
│
├── applications
│   ├── /
│   ├── /new
│   └── /:id
│
├── git
│   ├── /projects
│   ├── /projects/:id
│   ├── /groups
│   └── /groups/:id
│
├── workspace
│   ├── /rag
│   └── /index
│
└── settings

⸻

五十七、第一阶段开发顺序

不要让 Codex 一次把所有东西全部实现。

按照下面顺序开发。

Phase 1

基础框架：

React
Go
SQLite
API
Layout
Router

完成：

Dashboard
Sidebar
Header

⸻

Phase 2

配置文件：

Config CRUD
File Read
File Write
Monaco Editor

这是第一个完整闭环。

⸻

Phase 3

日志：

Log Group
Log File
Log Viewer
Tail
Search

⸻

Phase 4

应用管理：

Application CRUD
Start
Stop
Restart
PID
Process Monitor

这是整个系统最重要的后端模块。

⸻

Phase 5

Git：

Scan
Project Discovery
Project CRUD
Group

⸻

Phase 6

Workspace：

RAG directory
Index directory
Group Context

⸻

Phase 7

AI：

Index
Embedding
RAG
AI Query
AI Code Modification

⸻

五十八、第一版明确不做的事情

为了控制复杂度，V1 不做：

Docker 管理
Kubernetes 管理
远程服务器管理
SSH
远程 Git 操作
AI Chat
Embedding
Vector Database
复杂权限系统
用户系统
账号登录
云同步

第一版只解决：

我的 Mac
+
我的开发项目
+
我的配置
+
我的日志
+
我的本地应用
+
我的 Git 项目

⸻

五十九、V1 最终产品形态

最终打开：

http://127.0.0.1:xxxx

看到：

┌──────────────────────────────────────────────────────┐
│ Local Dev Workspace                                  │
├──────────────┬───────────────────────────────────────┤
│              │                                       │
│ Dashboard    │                                       │
│              │       Dashboard                       │
│ Config       │                                       │
│              │       Running Apps                    │
│ Logs         │                                       │
│              │       Recent Projects                 │
│ Applications │                                       │
│              │       Recent Logs                     │
│ Git Projects │                                       │
│              │                                       │
│ Workspace    │                                       │
│              │                                       │
│ Settings     │                                       │
│              │                                       │
└──────────────┴───────────────────────────────────────┘

最终目标不是：

「一个可以管理配置文件的网站」

而是：

一个运行在本机上的开发环境控制中心。

后续可以自然演进成：

                 Local Dev Workspace
                         │
        ┌────────────────┼────────────────┐
        │                │                │
     System           Project            AI
        │                │                │
   ┌────┼────┐      ┌────┼────┐      ┌────┼────┐
   │    │    │      │    │    │      │    │    │
 Config Log App    Git Group Files   RAG Index Agent

其中最重要的长期演进路线是：

本地文件管理
      ↓
本地应用管理
      ↓
Git 项目管理
      ↓
项目分组
      ↓
项目 Context
      ↓
RAG / Index
      ↓
AI 理解项目
      ↓
AI 修改项目
      ↓
AI 开发工作台

因此第一版在设计上应该重点保证 Application / Git Project / Group / Workspace Context 这几个领域模型的可扩展性，而不是过早把 AI 功能实现进去。

我额外建议 Codex 特别注意的 4 个点

第一，数据库用 SQLite。 你的场景完全没有 MongoDB 的必要，SQLite 反而能让整个应用真正做到“单机一个程序即可运行”。

第二，进程管理不要只做 exec.Command + PID。 这是这个项目里最容易踩坑的地方，尤其是 macOS 下 pnpm → node → vite、npm → node、go run → 子进程 这种进程树。要把 ProcessManager 单独做成后端核心模块。

第三，Git 项目和 Group 是以后 AI 能力的核心数据模型。 我建议不要把“RAG 文件夹”和“Index 文件夹”理解成简单的两个目录，而应该把它们视为一个 ProjectContext / GroupContext 的物理存储层。以后 AI Index、Embedding、代码知识库都可以挂在这里。

第四，第一版先不要上 WebSocket。 应用状态目前 5 秒轮询完全够用；日志先做文件读取 + tail。等基本架构跑通以后，再把实时进程状态、实时日志统一升级成 SSE/WebSocket，会更稳。

这套架构比较适合你现在的 React + TypeScript + Go + macOS 开发环境，也不会把第一版做得过重。